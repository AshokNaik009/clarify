---
description: "Supervised, goal-narrowed execution: pick the next goal-aligned AC, checkpoint with the user, implement, evaluate, repeat. Peer to `clarify ralph`."
---

Read the file at `${CLAUDE_PLUGIN_ROOT}/skills/goal/SKILL.md` using the Read tool and follow its instructions exactly.

## User Input

{{ARGUMENTS}}
