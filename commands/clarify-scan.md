---
description: "Snapshot the project's manifests and a structured codebase summary into .clarify/state.json (state.scan). Brownfield prep step."
---

Read the file at `${CLAUDE_PLUGIN_ROOT}/skills/scan/SKILL.md` using the Read tool and follow its instructions exactly.

## User Input

{{ARGUMENTS}}
