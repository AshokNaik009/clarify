---
description: "Author seed.mechanical_checks (lint/typecheck/test/build) from the project's manifests via one LLM call."
---

Read the file at `${CLAUDE_PLUGIN_ROOT}/skills/detect/SKILL.md` using the Read tool and follow its instructions exactly.

## User Input

{{ARGUMENTS}}
